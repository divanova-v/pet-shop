{% extends 'base.html.twig' %}
{% block main %}
    <h1>Productcategory edit</h1>
<div class="row">
    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-info" type="submit" value="Edit" />
        <a class="btn btn-default" role="button" href="{{ path('product-category_index') }}">Back to the list</a>
    {{ form_end(edit_form) }}
</div>
{% endblock %}
